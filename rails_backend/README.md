# RAILS
```
$ rails new rails_backend --api -d postgresql --skip-git
$ cd rails_backend
$ rails db:create
$ rails dbconsole
$ rails s -p 4002
$ rails generate migration CreateStocks
```

```
$ rails dbconsole
$ SELECT * FROM stocks;
$ rails db:seed
```
